# Standards: 0.11
---

# 1.1.8 Ensure nodev option set on /var/tmp partition

- name: regather facts
  setup:

- name: 1.1.8 - Ensure nodev option set on /var/tmp partition
  mount:
    name: "{{ item.mount }}"
    state: mounted
    fstype: "{{ item.fstype }}"
    src: "{{ item.device }}"
    opts: "{{ item.options.split(',') | union(['nodev']) | join(',') }}"
  when: item.mount == '/var/tmp'
  with_items: "{{ ansible_local.all_mounts }}"
  tags:
    - level-1
    - section-1
    - "1.1.8"
    - scored